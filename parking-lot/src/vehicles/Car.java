package vehicles;

import enums.VehicleType;

public class Car extends Vehicle {

    public Car(String licensePlate, VehicleType type, boolean isDisabled) {
        super(licensePlate, type, isDisabled);
    }
}

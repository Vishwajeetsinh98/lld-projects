package vehicles;

import enums.VehicleType;

public class MotorCycle extends Vehicle {
    public MotorCycle(String licensePlate, VehicleType type, boolean isDisabled) {
        super(licensePlate, type, isDisabled);
    }
}

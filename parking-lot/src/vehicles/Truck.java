package vehicles;

import enums.VehicleType;

public class Truck extends Vehicle {
    public Truck(String licensePlate, VehicleType type, boolean isDisabled) {
        super(licensePlate, type, isDisabled);
    }
}

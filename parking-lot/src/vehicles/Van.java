package vehicles;

import enums.VehicleType;

public class Van extends Vehicle {

    public Van(String licensePlate, VehicleType type, boolean isDisabled) {
        super(licensePlate, type, isDisabled);
    }
}
